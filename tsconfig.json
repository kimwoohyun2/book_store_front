{
  "compilerOptions": {
    // 'es3', 'es5', 'es2015', 'es2016', 'es2017','es2018', 'esnext' 가능
    "target": "es5",
    //무슨 import 문법 쓸건지 'commonjs', 'amd', 'es2015', 'esnext'
    "module": "esnext",
    // js 파일들 ts에서 import해서 쓸 수 있는지
    "allowJs": true,
    // .tsx 파일에서 JSX 지원
    "jsx": "react-jsx",
    // 빌드아웃풋 경로바꾸기
    "outDir": "./dist",
    "lib": ["dom", "dom.iterable", "esnext"],
    "skipLibCheck": true,
    "esModuleInterop": true,
    // default export가 없는 모듈에서 default imports를 허용합니다. 코드 방출에는 영향을 주지 않으며, 타입 검사만 수행합니다.
    "allowSyntheticDefaultImports": true,
    // strict 모두 여부
    "strict": true,
    // 동일 파일 참조에 대해 일관성 없는 대소문자를 비활성화한다.
    "forceConsistentCasingInFileNames": true,
    // switch문 이상하면 에러내기
    "noFallthroughCasesInSwitch": true,
    // 모듈 해석 방법 결정. Node.js 스타일 해석의 경우, "Node" 또는 "Classic" 중 하나
    "moduleResolution": "node",
    // .json 확장자로 import된 모듈을 포함한다.
    "resolveJsonModule": true,
    // 추가 검사를 수행하여 별도의 컴파일이 안전한지 확인한다.
    //  -> 예를 들어 트랜스파일된 모듈 혹은 @babel/plugin-transform-typescript
    "isolatedModules": true,
    // 출력을 내보내지 않음
    "noEmit": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
